#snb

stages:
  - build

build_backend_job:
  stage: build
  script:
    - docker build -f backend/Dockerfile -t registry.gitlab.com/opensi/snb-backend:via-docker
    - docker push registry.gitlab.com/opensi/snb-backend
  tags:
    - via-docker



